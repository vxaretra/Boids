#pragma once

static int NUM_OF_BOIDS = 50;

static int SCREEN_WIDTH = 1280;
static int SCREEN_HEIGHT = 720;

static float SCREEN_CENTER_X = SCREEN_WIDTH / 2.0f;
static float SCREEN_CENTER_Y = SCREEN_HEIGHT / 2.0f;
